
Go to:

👉 EC2 → Master Instance and worker nodes(→ Security Group → Inbound Rules

Add these rules:
1️⃣ Kubernetes API access
Type: all TCP
Port: all
Source: Worker node private IP OR same Security Group

==========only in master node===========
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config

kubectl apply -f https://docs.projectcalico.org/manifests/calico.yaml ---------->  in master node



copy join token from master node and paste in worker nodes

Check Nodes 
=============

kubectl get nodes
